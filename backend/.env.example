DATABASE_URL="file:./dev.db"
PORT=3001
# Directory where uploaded product photos are written/served from (relative
# to the backend project root, or an absolute path). In docker-compose this
# is set to /app/data/uploads so uploads persist in the backend-data volume.
UPLOADS_DIR="./uploads"
